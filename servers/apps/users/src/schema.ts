import { gql } from 'apollo-server-express';

export const typeDefs = gql`
  type Query {
    // Таны схемийн тодорхойлолтууд энд байна
  }
  
  type Mutation {
    // Таны мутацийн тодорхойлолтууд энд байна
  }
`;
